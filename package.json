{
  "name": "ldcn-final",
  "private": true,
  "workspaces": [
    "core",
    "apps/api",
    "apps/web",
    "apps/marketplace"
  ],
  "scripts": {
    "test": "npm test -w core && npm test -w apps/api && npm test -w apps/web && npm test -w apps/marketplace",
    "build": "npm run build -w core && npm run build -w apps/api && npm run build -w apps/web && npm run build -w apps/marketplace"
  }
}
